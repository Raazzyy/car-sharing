name: yolda_driver_app
description: YO'LDA B2B Carsharing Driver Application (Uzbekistan).
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  http: ^1.2.0
  google_maps_flutter: ^2.5.3
  intl: ^0.19.0
  flutter_map: ^8.3.1
  latlong2: ^0.10.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
